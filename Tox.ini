[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    pytest-cov
    datetime
    pytz
commands = pytest --cov=FileShare --cov-report=xml --cov-config=tox.ini --cov-branch
 
[coverage:run]